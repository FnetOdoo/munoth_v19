<odoo>
    <record id="res_config_settings_view_form_purchase_inherit" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.purchase</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="25"/>
        <field name="inherit_id" ref="purchase.res_config_settings_view_form_purchase"/>
        <field name="arch" type="xml">
            <!-- Insert after existing Products block -->
            <xpath expr="//block[@name='matrix_setting_container']" position="after">
                <block title="Supplier Discount" name="supplier_discount_settings_container">
                    <setting id="purchase_supplier_discount_real_setting"
                             string="Supplier Discount on Purchase"
                             help="If enabled, the discounts on each line will be updated to the supplier's discount or zero if no rate exists.">

                        <field name="purchase_supplier_discount_real"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>